# OpenAI API key for the résumé AI assistant.
# Server-side only — NEVER prefix with NEXT_PUBLIC_.
# Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: pin a specific OpenAI model id.
# Defaults to "gpt-5.6-luna" (see lib/openai.ts).
OPENAI_MODEL=
