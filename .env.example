# Google AI Studio key for the résumé AI assistant.
# Server-side only — NEVER prefix with NEXT_PUBLIC_.
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: pin a specific Gemini model id.
# Defaults to "gemini-flash-lite-latest" (see lib/gemini.ts).
GEMINI_MODEL=
